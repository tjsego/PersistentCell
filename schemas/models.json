{
  "MODEL000": {
    "method": "CPM",
    "description": "No persistence"
  },
  "MODEL001": {
    "method": "CPM",
    "description": "Equation 2 in 10.1084/jem.20061278"
  },
  "MODEL002": {
    "method": "CPM",
    "description": "Equation 3 in 10.1007/978-3-7643-8123-3_7"
  },
  "MODEL003": {
    "method": "CPM",
    "description": "Constant persistence as implemented by ExternalPotentialPlugin in CompuCell3D",
    "model_args": {
      "lambda_dir": {
        "description": "Lagrange multiplier of the work term, controls the cell speed.",
        "type": "number"
      },
      "target_angle": {
        "description": "Angle (to the positive x-axis) of the fixed target direction.",
        "type": "number"
      },
      "cpm_force_mode": {
        "description": "Work term for movement along a given direction.",
        "type": "string",
        "enum": ["extension", "retraction", "reciprocal"]
      },
      "cpm_update_direction": {
        "description": "Cell movement that would be induced by the proposed copy attempt.",
        "type": "string",
        "enum": ["source-to-target-unnorm", "source-to-target-norm", "cell-mass-displacement"]
      }
    }
  },
  "MODEL004": {
    "method": "CM",
    "description": "Equation 9 in 10.1371/journal.pcbi.1005991"
  },
  "MODEL005": {
    "method": "CPM",
    "description": "Simple self-reinforcing PRW in the CPM",
    "model_args": {
      "lambda_dir": {
        "description": "Lagrange multiplier of the work term, controls the cell speed.",
        "type": "number"
      },
      "dt": {
        "description": "Time interval (in MCS) over which we evaluate the cell's recent displacement; this determines persistence time of the random walk.",
        "type": "number"
      },
      "cpm_force_mode": {
        "description": "Work term for movement along a given direction.",
        "type": "string",
        "enum": ["extension", "retraction", "reciprocal"]
      },
      "cpm_update_direction": {
        "description": "Cell movement that would be induced by the proposed copy attempt.",
        "type": "string",
        "enum": ["source-to-target-unnorm", "source-to-target-norm", "cell-mass-displacement"]
      }
    }
  },
  "MODEL006": {
    "method": "CPM",
    "description": "Persistent motion controlled by cell-intrinsic orientation under effect of continuous time noise",
    "model_args": {
      "lambda_dir": {
        "description": "Lagrange multiplier of the work term, controls the cell speed.",
        "type": "number"
      },
      "xi": {
        "description": "Standard deviation of noise added to the cell direction (larger value implies lower persistence time).",
        "type": "number"
      },
      "cpm_force_mode": {
        "description": "Work term for movement along a given direction.",
        "type": "string",
        "enum": ["extension", "retraction", "reciprocal"]
      },
      "cpm_update_direction": {
        "description": "Cell movement that would be induced by the proposed copy attempt.",
        "type": "string",
        "enum": ["source-to-target-unnorm", "source-to-target-norm", "cell-mass-displacement"]
      }
    }
  },
  "MODEL007": {
    "method": "CPM",
    "description": "Persistent motion controlled by cell-intrinsic orientation under effect of continuous time noise and self-reenforcement of direction of motion"
  }
}
